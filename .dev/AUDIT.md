# Project Audit

## Phase 1: Baseline Assessment
**Date:** 2025-05-15
**Project:** Data Hygiene Auditor

### What Was Intended
A tool that highlights which fields in a dataset are dirty — mixed formats, misused fields, placeholder values, phantom duplicates. Originally generated by Claude Chat. Intended to be useful for the author's consulting work, impressive enough to hand to clients, and strong as a portfolio piece.

### What Exists Today
A single-file Python CLI (`audit.py`, ~1,200 LOC) that reads an Excel workbook and produces three report formats (HTML, Excel findings, PDF). It detects 5 categories of issues: mixed formats (date/phone/currency), misused fields, placeholder floods, phantom duplicates, and completeness gaps. Each finding gets a severity rating and a plain-English "why this matters" explanation.

A sample data generator (`generate_sample.py`) creates a deliberately messy 30-row, 2-sheet workbook for demo purposes. Pre-generated sample outputs are committed to the repo.

The tool has only been run against the sample file — never tested on real-world data.

### Tech Stack
- **Language:** Python 3.8+
- **Dependencies:** pandas, openpyxl, reportlab (3 total, pinned by name only — no versions)
- **Entry point:** `python audit.py --input <file> --output <dir>`
- **No framework** — pure stdlib + 3 libraries

### Project Health Indicators
- **Activity:** Shipped but neglected. 8 commits total, most recent was a bug-fix PR. No ongoing development.
- **Documentation:** README is solid — clear explanation, usage examples, screenshots. No API docs, no CLAUDE.md, no contributing guide.
- **Test coverage:** Zero. No test files, no test framework in dependencies.
- **Dependencies:** Only 3, all mainstream and stable. No version pins — risk of breakage on major updates.
- **CI/CD:** None. No GitHub Actions, no linting, no automated checks.
- **Code quality:** Single 1,200-line file mixing analysis logic, HTML generation (inline CSS/HTML strings), Excel formatting, and PDF generation. No separation of concerns. Functional but monolithic.

### Gap Analysis

**What works:**
- Core detection logic covers real consulting pain points
- Three output formats tailored to different audiences is genuinely useful
- HTML report looks polished (dark theme, severity badges, progress bars)
- "Why this matters" explanations are a strong differentiator for non-technical stakeholders

**What's missing or weak:**
1. **No tests.** Zero confidence that changes won't break things. Can't validate detection accuracy against known inputs.
2. **Never tested on real data.** The sample file is synthetic — real-world spreadsheets are messier and larger in ways the tool may not handle.
3. **Monolithic architecture.** Everything in one file makes it hard to extend, test, or maintain.
4. **No error handling for edge cases.** Large files, corrupt data, password-protected sheets, non-.xlsx formats — unknown behavior.
5. **No CI.** No linting, no type checking, no automated test runs.
6. **No version pinning.** `requirements.txt` has bare package names.
7. **The "lame" factor.** The tool does what it says, but there's nothing that makes someone say "wow." It's competent but not memorable — a problem when it's also a portfolio piece.
8. **No CSV support.** Only works with .xlsx despite CSV being extremely common for dirty data.
9. **No interactivity.** CLI-only, no way to preview or explore findings before generating full reports.

### Audit Motivation
The author finds the project "lame" despite it being functional. The goal of this audit is to identify what would transform it from a Claude Chat artifact into something genuinely impressive — useful enough for real consulting work, polished enough to show clients, and distinctive enough to stand out in a portfolio.

## Phase 2: Internal Review
**Date:** 2025-05-15
**Dimensions reviewed:** Code Quality, Architecture, Tests, Documentation, Performance, Security, UX, DevEx

### Top Opportunities (by leverage)

| # | Finding | Dimension | Impact | Effort | Leverage | Severity |
|---|---------|-----------|--------|--------|----------|----------|
| 1 | Escape "why" text in HTML/PDF — latent XSS and XML injection | Security | 4 | 1 | 4.0 | important |
| 2 | Extract issue-counting into helper (duplicated 3x) | Code Quality | 3 | 1 | 3.0 | important |
| 3 | Fix HTML string concatenation → list + join (quadratic perf) | Performance | 3 | 1 | 3.0 | important |
| 4 | Add progress indicator for large files | UX | 3 | 1 | 3.0 | important |
| 5 | Pin dependency versions in requirements.txt | DevEx | 3 | 1 | 3.0 | important |
| 6 | Fix blank_count/whitespace_only overlap logic | Code Quality | 3 | 1 | 3.0 | important |
| 7 | Validate input file type (.xlsx/.csv) | UX | 3 | 1 | 3.0 | important |
| 8 | Remove dead code (SEVERITY_COLORS dict unused) | Code Quality | 1 | 1 | 1.0 | minor |
| 9 | Add CSV input support | UX | 4 | 2 | 2.0 | important |
| 10 | Set up CI with GitHub Actions | DevEx | 4 | 2 | 2.0 | important |
| 11 | Pre-compile and combine regex patterns | Performance | 3 | 2 | 1.5 | important |
| 12 | Add test suite with pytest | Tests | 5 | 3 | 1.7 | critical |
| 13 | Vectorize phantom duplicate detection | Performance | 5 | 3 | 1.7 | critical |
| 14 | Interactive HTML report (filtering, TOC, search) | UX | 4 | 3 | 1.3 | important |
| 15 | Split into package structure | Architecture | 4 | 3 | 1.3 | important |
| 16 | Typed data models (dataclasses) for findings | Architecture | 5 | 4 | 1.3 | critical |
| 17 | Unified renderer dispatch (stop duplicating per-type logic) | Architecture | 5 | 4 | 1.3 | critical |
| 18 | Add type hints throughout | Documentation | 3 | 3 | 1.0 | important |

### Detailed Findings

#### Code Quality
- **[important]** `blank_count` and `whitespace_only` at `audit.py:153-154` use overlapping predicates — whitespace_only is always a subset of blank_count, but they're reported as if disjoint.
- **[important]** Issue-counting loop (total_issues + severity_totals) is copy-pasted identically at lines 611-620, 1038-1047, and 1210-1219. Must update all three if findings structure changes.
- **[important]** Issue-type-to-description rendering switch (mixed_format / wrong_purpose / placeholder / etc.) is repeated across HTML (814-841), Excel (920-937), and PDF (1093-1123) generators with slight formatting variations. Adding a new issue type requires edits in 4 places.
- **[important]** `why` text fallback at line 568 silently uses a date-specific explanation for unrecognized format types.
- **[minor]** `_h` and `_p` (lines 35, 41) are cryptic single-letter names — `html_esc` / `pdf_esc` would be self-documenting.
- **[minor]** Magic numbers: sample size `200` at line 106, threshold `0.5` at lines 131-140.
- **[minor]** `SEVERITY_COLORS` dict at lines 89-93 is defined but never referenced — dead code.
- **[residual risk]** Currency pattern `^\d+$` (line 82) matches any bare integer. A pure-numeric column inferred as currency would flag every value as valid currency format.
- **[residual risk]** If a sheet has columns but zero non-null values across all of them, phantom duplicate analysis hashes empty strings, potentially reporting every row as a duplicate.

#### Architecture
- **[critical]** All logic in one 1,227-line file with no structural boundaries. HTML generator alone is 266 lines of inline string concatenation with embedded CSS.
- **[critical]** `run_audit` returns a deeply nested dict-of-dicts with no schema. Renderers reach into `detail['format_distribution']`, `detail['value']`, etc. with no type safety. A key rename silently breaks all three renderers.
- **[critical]** No plugin or registry pattern for detection rules. Adding a new rule requires edits to: engine function, `run_audit` wiring, `rate_severity`, `WHY_IT_MATTERS`, and all three renderers.
- **[important]** Should be a package: `detection/` (engines), `reporting/` (HTML, Excel, PDF), `models.py` (dataclasses), `cli.py` (entrypoint).

#### Tests
- **[critical]** Zero test files, no test framework in dependencies. No way to validate detection accuracy or prevent regressions.
- **[critical]** No tests for edge cases: single-row sheets, all-ID-column sheets, entirely null sheets, columns where every value is unique.
- **[important]** `generate_sample.py` is the closest thing to a test fixture but isn't used in any automated test.

#### Documentation
- **[positive]** README is well-written with clear explanation, usage examples, and screenshots.
- **[positive]** `samples/README.md` provides good context for the demo outputs.
- **[important]** No type hints anywhere — makes it hard to understand function contracts or use IDE tooling.
- **[minor]** No CHANGELOG, CONTRIBUTING guide, or API documentation.

#### Performance
- **[critical]** `analyze_phantom_duplicates` (line 379) uses `.apply(lambda, axis=1)` for row-by-row MD5 hashing — pure Python loop, no vectorization. At 100K rows this dominates runtime; at 1M rows it's a wall (estimated 10+ minutes).
- **[important]** `analyze_mixed_formats` (lines 184-190) tests every value against up to 9 regex patterns sequentially. Pre-compiling into combined patterns would cut this significantly.
- **[important]** HTML generation via `html +=` string concatenation (lines 622-871) is quadratic — each append copies all prior content. Switch to list + `''.join()`.
- **[important]** `dtype=str` (line 528) loads every cell as a Python string object. A 500K-row, 50-column file creates 25M objects with no chunking.
- **[minor]** `analyze_nulls` (lines 153-154) uses Python generator expressions instead of vectorized `series.str.strip().eq('')`.
- **[minor]** Each analysis engine independently materializes its own filtered `non_null` list of the same column data.

#### Security
- **[important]** `why` text inserted raw into HTML at lines 844 and 864 without calling `_h()`. Currently safe (hardcoded strings), but latent XSS if the results dict is ever populated from external sources.
- **[important]** Same pattern in PDF at lines 1127 and 1137 — `why` passed to `Paragraph()` without `_p()`, allowing XML tag injection in reportlab.
- **[minor]** No row-limit or file-size check — a crafted xlsx with millions of rows or a zip bomb could exhaust memory.
- **[minor]** Unpinned dependencies. Older reportlab versions had XML parsing issues.
- **[minor]** MD5 at line 379 is fine for checksumming but flagged by security linters.

#### UX
- **[important]** No progress indicator. On a large file, the user sees nothing until all three reports are generated.
- **[important]** No input validation — non-.xlsx files, directories, or missing files get unhelpful errors.
- **[important]** HTML report is static — no table of contents, no filtering by severity, no search. For a report with 100+ issues, this is unusable.
- **[important]** No CSV support despite CSV being the most common dirty-data format.
- **[minor]** No `--quiet` or `--verbose` flags. No color in terminal output.

#### DevEx
- **[important]** No CI/CD — no GitHub Actions, no automated test runs, no linting on push.
- **[important]** No `pyproject.toml` or `setup.py` — can't install as a package or use standard Python tooling.
- **[important]** Dependency versions unpinned — `pip install -r requirements.txt` installs whatever's latest.
- **[minor]** No linting config (ruff, flake8), no formatting config (black), no pre-commit hooks.

### Summary
The core detection logic is sound and covers real consulting pain points, but the project's internals are a textbook example of "generated and shipped without iteration." The highest-leverage wins are cheap fixes: escaping the `why` text (security), extracting duplicated counting logic, and fixing the HTML concatenation pattern. The highest-impact wins require more effort but transform the project: a test suite, vectorized duplicate detection, and an interactive HTML report. The architecture (monolithic file, untyped dicts, triplicated rendering logic) is the root cause of most maintainability issues and should be addressed before adding features.

## Phase 3: Landscape Scan
**Date:** 2025-05-15
**Category:** Data quality auditing / data profiling tools for tabular data (Excel, CSV)
**Note:** WebSearch was unavailable; findings are from training knowledge (reliable for this mature category). Star counts and traction signals are mid-2025 estimates — verify before publishing.

### Competitors / Similar Projects

| # | Name | Type | Description | Traction |
|---|------|------|-------------|----------|
| 1 | **ydata-profiling** (fka pandas-profiling) | OSS Python | Auto-generates comprehensive HTML profile report from a DataFrame — nulls, distributions, correlations, duplicates, alerts | ~13K GitHub stars, VC-backed (YData), most-downloaded data quality package on PyPI |
| 2 | **Great Expectations** | OSS Python + Commercial | Rule-based data validation framework — define "expectations," run them, get HTML Data Docs site | ~10K stars, $40M Series B, GX Cloud product |
| 3 | **OpenRefine** | OSS Java (GUI) | Browser-based tool for exploring/cleaning messy tabular data — faceting, text clustering, fuzzy dedup | ~10.5K stars, 15+ years old, de facto standard for hands-on data cleaning |
| 4 | **pandera** | OSS Python | Declarative schema validation for pandas/Polars DataFrames — types, ranges, regex, nullable checks | ~3.5K stars, maintained by Union.ai |
| 5 | **Soda Core** | OSS Python + Commercial | YAML-driven data quality checks (SodaCL) for SQL/Spark/files — anomaly detection, monitoring, alerting | ~1.5K stars, $18M Series A, SaaS platform |
| 6 | **AWS Deequ** | OSS Scala/Python | Unit tests for data at Amazon scale — metrics, constraint suggestion, anomaly detection with time series | ~3.3K stars, AWS-maintained |
| 7 | **frictionless-py** | OSS Python | Validates tabular files (CSV, Excel) against JSON Table Schema — cell-level errors with row/col coordinates | ~1.7K stars, Open Knowledge Foundation |
| 8 | **Talend / Qlik** | Commercial | Enterprise ETL + data quality platform — profiling, standardization, record linkage, dedup | Acquired for ~$3B, tens of thousands of enterprises |

### Feature Matrix

| Feature | This Project | ydata-profiling | Great Expectations | OpenRefine | frictionless-py | pandera | Soda Core |
|---------|:-----------:|:-----------:|:-----------:|:-----------:|:-----------:|:-----------:|:-----------:|
| **Excel native input** | ✅ | ❌ (needs DataFrame) | ❌ (needs DataFrame) | ✅ | ✅ | ❌ | ❌ |
| **CSV support** | ❌ | ✅ (via pandas) | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Multi-sheet support** | ✅ | ❌ | ❌ | ❌ | 🟡 | ❌ | ❌ |
| **Auto field type inference** | ✅ | ✅ | 🟡 | ✅ | ✅ | ❌ (manual) | ❌ |
| **Mixed format detection** (cell-level) | ✅ | ❌ | ❌ | 🟡 (via facets) | ❌ | ❌ | ❌ |
| **Placeholder/test data detection** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **Phantom/fuzzy duplicate detection** | ✅ (case/whitespace) | 🟡 (exact only) | ❌ | ✅ (clustering algos) | ❌ | ❌ | ❌ |
| **Null/completeness analysis** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Misused field detection** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **Severity ratings** | ✅ | 🟡 (alerts) | ❌ | ❌ | ❌ | ❌ | ❌ |
| **Plain-English explanations** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **HTML report** | ✅ | ✅ | ✅ (Data Docs) | ➖ (is the UI) | ❌ (JSON) | ❌ | ❌ |
| **Excel findings output** | ✅ | ❌ | ❌ | ✅ (export) | ❌ | ❌ | ❌ |
| **PDF report** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| **Interactive/filterable report** | ❌ | ✅ | ✅ | ✅ (is interactive) | ❌ | ❌ | ✅ (cloud) |
| **Statistical profiling** (distributions, correlations) | ❌ | ✅ | 🟡 | 🟡 | 🟡 | ❌ | ❌ |
| **Schema validation** (explicit rules) | ❌ | ❌ | ✅ | ❌ | ✅ | ✅ | ✅ |
| **CLI interface** | ✅ | 🟡 | ✅ | ❌ (GUI) | ✅ | ❌ (API) | ✅ |
| **Python API / importable** | ❌ | ✅ | ✅ | ❌ | ✅ | ✅ | ✅ |
| **Pipeline/CI integration** | ❌ | ❌ | ✅ | ❌ | 🟡 | ✅ | ✅ |
| **Auto-fix suggestions** | ❌ | ❌ | ❌ | ✅ (transforms) | ❌ | ❌ | ❌ |
| **Large file handling** (100K+ rows) | ❌ | 🟡 | ✅ | ✅ | ✅ | ✅ | ✅ |
| **No-code / GUI** | ❌ | ❌ | ❌ | ✅ | ❌ | ❌ | 🟡 (cloud) |

### Landscape Position

#### Table Stakes (standard — are any missing here?)
- **CSV support** — Every competitor handles CSV. This project only does .xlsx. **Missing.**
- **Null/completeness analysis** — Universal. This project has it. ✅
- **CLI or API interface** — Standard. This project has CLI. ✅
- **Large file handling** — Most competitors handle 100K+ rows. This project will struggle. **Weak.**
- **Importable as a library** — Most Python tools can be used programmatically. This project is CLI-only. **Missing.**

#### Where This Project Is Stronger
- **Mixed format detection** — Cell-level detection of date/phone/currency format inconsistencies within a single column. No competitor does this well. ydata-profiling detects type mismatches but not format-level inconsistency (e.g., "2023-01-15" vs "Jan 15, 2023" in the same column).
- **Placeholder/test data detection** — Specific detection of "Test", "N/A", "TBD", "000-000-0000" and other consulting-reality patterns. Nobody else flags these.
- **Misused field detection** — Reference codes in name fields, text in currency columns. Unique to this project.
- **Triple output format** — HTML + Excel findings + PDF from one run. No competitor produces all three.
- **Severity ratings with plain-English explanations** — Issue categorization with business-context "why this matters" text. Designed for non-technical stakeholders. No competitor does this.
- **Excel-native multi-sheet** — Reads .xlsx directly, audits all sheets independently. ydata-profiling and Great Expectations require loading into a DataFrame first.

#### Where This Project Is Weaker
- **No interactivity** — ydata-profiling, Great Expectations, and OpenRefine all produce interactive, filterable reports. This project's HTML is static.
- **No statistical profiling** — No distributions, correlations, or outlier detection. ydata-profiling's core strength.
- **No schema validation** — Can't define explicit rules. Great Expectations, pandera, and Soda Core are built around this.
- **No pipeline integration** — Can't plug into CI/CD, dbt, or Airflow. Major gap for data engineering use cases.
- **No fuzzy matching algorithms** — Phantom duplicate detection uses exact-after-normalization matching. OpenRefine uses Levenshtein, fingerprint, metaphone, and nearest-neighbor clustering — far more sophisticated.
- **Performance at scale** — Will choke on large files where competitors handle them routinely.

#### Unique Differentiators
These features exist **only** in this project among the tools surveyed:
1. **Placeholder/test data detection** — Nobody else specifically flags "TBD", "Test", "N/A" as production data quality issues.
2. **Misused field detection** — Cross-column semantic validation (codes in name fields, text in currency columns) with automatic field type inference.
3. **Consulting-ready triple output** — HTML for the meeting, Excel for the cleanup person, PDF for the deliverable folder. This is workflow-aware, not just technically complete.
4. **Severity + business-context explanations** — Findings rated and explained for non-technical stakeholders. Every other tool reports to engineers.

#### Category Trends
- **AI-powered suggestions** — Newer tools are adding LLM-powered fix recommendations ("this looks like a date, did you mean YYYY-MM-DD?"). Nobody does this well yet.
- **Observability/monitoring** — Soda, Great Expectations, and commercial tools are moving toward continuous data quality monitoring, not one-shot audits.
- **Data contracts** — Schema-as-code (pandera, Soda) is gaining traction, especially in data mesh architectures.
- **Interactive exploration** — Static reports are being replaced by explorable, filterable interfaces.
- **The consultant gap persists** — Enterprise tools cost $50K+/year. Python libraries target engineers. The "consultant with a messy spreadsheet" persona remains underserved by purpose-built tools.

### Summary
Data Hygiene Auditor occupies a genuinely underserved niche: **Excel-native, consultant-focused data quality auditing with business-readable output**. No existing tool combines automatic field type inference, cell-level format inconsistency detection, placeholder flagging, and triple-format reporting for non-technical stakeholders. The project's unique features (placeholder detection, misused fields, severity + explanations, triple output) are real differentiators, not gimmicks. But it's missing table-stakes capabilities (CSV support, large file handling, interactivity) that limit its credibility and utility. The "lame" feeling likely comes not from what it detects, but from how it presents — a static HTML page when the category has moved toward interactive, explorable reports.

## Phase 4: Differentiation & Next Moves
**Date:** 2025-05-15

### Cross-Reference Summary

The audit reveals an unusual situation: the project's **detection capabilities are genuinely differentiated** (placeholder detection, misused fields, severity + explanations — nobody else does these), but the project **doesn't feel differentiated** because the presentation layer and engineering foundation are below category standard. The "lame" feeling comes from the gap between what the tool *finds* and how it *communicates* those findings.

The internal issues (Phase 2) and competitive gaps (Phase 3) are deeply linked. The monolithic architecture blocks adding CSV support (close a gap), an importable API (close a gap), and new detection rules (extend a strength) — all at once. The static HTML report is both the biggest internal UX weakness and the biggest competitive gap. Fixing it doesn't just catch up to ydata-profiling; combining interactivity with severity ratings and plain-English explanations would create something no competitor has.

The strategic path is clear: **fix the foundation, close the table-stakes gaps, then invest in the presentation layer where your unique detection logic can finally shine.** The detection engines are the moat — they just need a stage that does them justice.

### Ranked Next Moves

| # | Move | Category | Strategic | Internal | Effort | Score | Description |
|---|------|----------|-----------|----------|--------|-------|-------------|
| 1 | CSV support | Close gap | 4 | 2 | 1 | 6.0 | Every competitor handles CSV. Trivial with pandas — just `pd.read_csv` alongside `pd.read_excel`. Unblocks the most common dirty-data format. |
| 2 | Quick fixes batch | Foundational | 1 | 4 | 1 | 5.0 | Escape `why` text (security), pin deps, fix blank/whitespace overlap, remove dead code, fix HTML string concat, validate input type. All 1-line changes. |
| 3 | Progress + colored CLI | Close gap | 3 | 2 | 1 | 5.0 | Progress bar (tqdm or simple counter) + colored severity output. CLI goes from "bare script" to "polished tool." |
| 4 | pyproject.toml + installable | Foundational | 2 | 3 | 1 | 5.0 | Standard Python packaging. Enables `pip install .`, entry point script, and signals "real project" to portfolio visitors. |
| 5 | Data quality health score | Double down | 5 | 2 | 2 | 3.5 | Single 0-100 "hygiene score" per sheet + overall. Quotable in meetings, trackable over time. Nobody else does this. The "wow" number at the top of the report. |
| 6 | CI/CD (GitHub Actions) | Foundational | 2 | 4 | 2 | 3.0 | Lint (ruff) + test on push. Badge in README. Portfolio credibility signal. |
| 7 | Interactive HTML report | Double down | 5 | 3 | 3 | 2.7 | Add filtering by severity, collapsible sections, TOC navigation, search. Transforms the static page into something you'd actually want to explore. This is the "lame" killer — combines your unique detection with modern presentation. |
| 8 | Package restructure | Foundational | 3 | 5 | 3 | 2.7 | Split into `detection/`, `reporting/`, `models.py`, `cli.py`. Unblocks: importable API, isolated testing, new detection rules, new output formats. |
| 9 | Vectorize phantom duplicates | Close gap + Double down | 4 | 4 | 3 | 2.7 | Replace row-by-row MD5 with `pd.util.hash_pandas_object` or vectorized ops. Fixes the 100K-row wall AND extends a unique feature. |
| 10 | Test suite | Foundational | 2 | 5 | 3 | 2.3 | pytest + tests for each detection engine using known-bad data. Enables safe iteration. |
| 11 | Fuzzy duplicate matching | Double down | 4 | 3 | 3 | 2.3 | Levenshtein / fingerprint clustering like OpenRefine. Upgrades phantom duplicate detection from "normalize and match" to real fuzzy dedup. |
| 12 | Importable Python API | Close gap | 3 | 3 | 2 | 3.0 | Expose `run_audit()` as a clean public API. Most Python tools offer this. Package restructure enables it for free. |
| 13 | AI-powered fix suggestions | Leapfrog | 5 | 1 | 4 | 1.5 | "This phone column has 7 formats. Here's a script to normalize to (XXX) XXX-XXXX." Category whitespace — nobody does this well yet. High effort, high risk, high reward. |

### Recommended Sequence

**Sprint 1: Quick Wins (1-2 days)**
Moves #1-4. All effort-1 items. Ship these and the project immediately feels more professional.
- CSV support
- Quick fixes batch (security, bugs, dead code)
- Progress bar + colored output
- pyproject.toml

**Sprint 2: Foundation (2-3 days)**
Moves #8, #10, #6. Build the engineering base that makes everything else safe and easy.
- Package restructure (must come before tests — otherwise you're testing a monolith)
- Test suite (lock in detection accuracy before changing anything)
- CI/CD (automate what you just built)

**Sprint 3: The "Wow" (3-5 days)**
Moves #5, #7, #9. This is where "lame" becomes "impressive."
- Data quality health score (the number at the top of every report)
- Interactive HTML report (filtering, search, collapsible sections)
- Vectorize phantom duplicates (handle real-world file sizes)

**Sprint 4: Differentiation (3-5 days)**
Moves #11, #12. Extend the moat.
- Fuzzy duplicate matching
- Importable Python API

**Future / Stretch:**
Move #13. AI-powered fix suggestions. Only attempt after the foundation and presentation are solid.

### What NOT to Do
- **Don't add schema validation.** Great Expectations, pandera, and Soda Core own this space with years of investment and massive communities. You can't out-schema them, and trying turns you into a worse version of them instead of a unique tool.
- **Don't add statistical profiling** (distributions, correlations, outlier detection). ydata-profiling does this comprehensively with 13K stars of momentum. Your strength is *consulting-specific* findings, not general statistics.
- **Don't build a GUI/web app yet.** The interactive HTML report gives you most of the "explorable" benefit without the deployment/hosting/auth complexity. A web app is a different product.
- **Don't chase pipeline integration** (dbt, Airflow, CI). Your audience is consultants with spreadsheets, not data engineers with warehouses. Pipeline integration dilutes your focus without serving your users.
- **Don't refactor before testing.** The temptation is to restructure first (it's messy!), but write tests against the current behavior first. Then refactor with confidence.

---

# Audit Round 2 (2026-05-16)

All items from the 2025 audit were shipped (PRs #1-#9). This round assesses the project's current state after that work, with fresh landscape data.

## Phase 1: Baseline Assessment (2026)
**Date:** 2026-05-16
**Project:** Data Hygiene Auditor v1.0.0

### What Exists Today

A well-structured Python CLI + library (10 modules, ~3,750 LOC) that scans Excel/CSV/TSV files for data quality issues and produces interactive HTML, Excel, and PDF reports. Features shipped since last audit: schema validation, trend comparison, vectorized detection (3.4x speedup), fuzzy duplicate matching, typed Python API, health scores, interactive HTML, fix suggestions.

### Current Architecture

| Module | LOC | Purpose |
|--------|-----|---------|
| `detection.py` | 654 | 7 detection engines |
| `reporting/html.py` | 841 | Interactive HTML report |
| `reporting/pdf.py` | 418 | PDF deliverable |
| `reporting/excel.py` | 335 | Excel findings file |
| `api.py` | 412 | Typed Python API (dataclasses) |
| `core.py` | 292 | Orchestrator + data loading |
| `suggestions.py` | 285 | Fix suggestion engine |
| `cli.py` | 202 | CLI with colored output |
| `schema.py` | 144 | Schema validation |
| `trend.py` | 103 | Trend comparison |
| **Tests** | 1,576 | 167 tests across 8 files |

### Health Indicators

| Dimension | Status |
|-----------|--------|
| Tests | 167 passing, all detection engines covered |
| CI | GitHub Actions: ruff + pytest on 3.9/3.12/3.13 |
| Packaging | pyproject.toml, pip-installable, `data-hygiene-audit` CLI |
| API | `audit_file()` with typed dataclasses, py.typed marker |
| Docs | Comprehensive README with screenshots and library examples |
| Performance | Vectorized detection, 3.4x improvement on large files |

### Gap Analysis

**Resolved from 2025 audit:** CSV support, tests, CI, packaging, interactive HTML, health score, vectorized perf, fuzzy matching, typed API, fix suggestions, schema validation, trend comparison — all shipped.

**Remaining or new issues:**
1. CLI under-counts issues (missing fuzzy duplicates in total)
2. `_raw` attribute set outside dataclass `__init__` — type-unsafe
3. Tests import via backward-compat shim, not package directly
4. No type checker in CI despite py.typed marker
5. Python 3.8 claimed but untested
6. Fuzzy matching silently skipped above 500 rows
7. No CHANGELOG or release tags

## Phase 2: Internal Review (2026)
**Date:** 2026-05-16
**Dimensions:** Code Quality, Architecture, Tests, Documentation, Performance, Security, UX, DevEx

### Top Opportunities

| # | Finding | Dimension | Impact | Effort | Leverage | Severity |
|---|---------|-----------|--------|--------|----------|----------|
| 1 | CLI missing fuzzy_duplicates in issue count — under-reports total | Code Quality | 3 | 1 | 3.0 | bug |
| 2 | `AuditResult._raw` monkey-patched outside `__init__` | Code Quality | 4 | 1 | 4.0 | important |
| 3 | Issue-counting logic still duplicated 3x (cli, html, excel) | Code Quality | 3 | 1 | 3.0 | important |
| 4 | `requires-python >= 3.8` but CI tests 3.9+ only | DevEx | 3 | 1 | 3.0 | important |
| 5 | Tests import from `audit` shim, not `data_hygiene_auditor` | Tests | 3 | 2 | 1.5 | important |
| 6 | No type checker in CI despite py.typed marker | DevEx | 3 | 2 | 1.5 | important |
| 7 | Levenshtein O(n²) hard-capped at 500 rows — silently skips | Performance | 3 | 3 | 1.0 | important |
| 8 | No file size guard — OOM on large crafted input | Security | 3 | 2 | 1.5 | important |
| 9 | `_load_sheets` exported in public `__all__` | Architecture | 2 | 1 | 2.0 | minor |
| 10 | `--schema`/`--baseline` undocumented in README options table | Documentation | 2 | 1 | 2.0 | minor |
| 11 | No `--quiet`/`--version` flags | UX | 2 | 1 | 2.0 | minor |
| 12 | No CHANGELOG | Documentation | 2 | 1 | 2.0 | minor |

### Summary

The project is in strong shape. The 2025 audit's critical issues (monolith, no tests, XSS, no CSV, no packaging) are all resolved. What remains is polish-tier work: a counting bug, a type safety issue, test import paths, and CI completeness. The architecture is clean and the detection logic is solid.

## Phase 3: Landscape Scan (2026)
**Date:** 2026-05-16
**Method:** Web research (verified through May 2026)

### Key Landscape Changes (2025 → 2026)

1. **ydata-profiling rebranded to fg-data-profiling** (v4.19.1, Apr 2026). Package/import renamed. Signals stewardship instability.
2. **GX added ExpectAI** — AI-generated expectations from data patterns. Possible acquisition May 2026 (unconfirmed).
3. **Data contracts became dominant framing** — Soda Core repositioned as "Data Contracts engine." Irrelevant to file-audit use case.
4. **Enterprise consolidation** — Metaplane → Datadog, SYNQ → Coalesce, Select Star → Snowflake. Affects $50K+ tier only.
5. **AI/LLM integration is commercial-tier only** — ExpectAI, SodaGPT. No OSS tool has AI fix suggestions. Window still open.
6. **DQX (Databricks Labs)** — new PySpark-native DQ framework. Not relevant to file-based auditing.
7. **DQOps** — OSS + commercial ($499/mo). 150+ built-in checks. Warehouse-only, no file support.

### Competitive Position (2026)

**Unique to this project (confirmed still unmatched):**
- Placeholder/test data detection
- Misused field detection (cross-column semantic validation)
- Triple output format (HTML + Excel + PDF)
- Severity ratings + plain-English explanations for non-technical stakeholders
- Health score (0-100)
- Deterministic fix suggestions with copy-paste code
- Schema validation + trend comparison (closes previous gaps)

**The consultant gap remains completely unoccupied.** Every competitor is a warehouse connector for engineers, a profiler for data scientists, or an interactive GUI for researchers. No tool takes a file and produces a credentialed audit report with severity ratings and fix language for a client meeting.

### Feature Parity Check

| Table Stakes | Status |
|-------------|--------|
| CSV/TSV support | ✅ Shipped |
| Null/completeness analysis | ✅ |
| CLI + Python API | ✅ Both |
| Large file handling (100K+) | 🟡 Vectorized but fuzzy capped at 500 |
| Interactive report | ✅ Filters, search, TOC, collapsible |

## Phase 4: Differentiation & Next Moves (2026)
**Date:** 2026-05-16

### Cross-Reference Summary

The situation has inverted since the 2025 audit. A year ago, the project had strong detection but weak everything else. Now:
- **Foundation:** solid (tests, CI, packaging, clean architecture)
- **Presentation:** strong (interactive HTML, health score ring, fix suggestions)
- **Detection:** comprehensive (7 engines + schema + trend)
- **Competitive position:** unique and uncontested

The remaining work is no longer transformative — it's **incremental quality improvements and strategic positioning**. The highest-impact moves are now about reach (getting the tool in front of users) and polish (fixing the few rough edges that undermine professional credibility).

### Ranked Next Moves

| # | Move | Category | Strategic | Internal | Effort | Score | Description |
|---|------|----------|-----------|----------|--------|-------|-------------|
| 1 | Fix CLI fuzzy dup counting bug | Correctness | 1 | 4 | 1 | 5.0 | CLI under-reports total issues by omitting fuzzy duplicates from count. One missing loop. |
| 2 | Fix `_raw` type safety | Code Quality | 1 | 3 | 1 | 4.0 | Move `_raw` into `AuditResult.__init__` as a proper field. Fixes mypy, IDE autocomplete. |
| 3 | Extract shared issue-counting helper | Code Quality | 1 | 3 | 1 | 4.0 | Single function used by CLI, HTML, and Excel. Prevents future counting bugs. |
| 4 | Document `--schema`/`--baseline` in README | Documentation | 2 | 2 | 1 | 4.0 | Features exist but aren't discoverable in README options table. |
| 5 | Add `--version` and `--quiet` flags | UX | 2 | 2 | 1 | 4.0 | Professional CLI conventions. `--quiet` enables scripted/CI usage. |
| 6 | Align Python version (drop 3.8 claim or add CI) | DevEx | 2 | 3 | 1 | 5.0 | Either add 3.8 to CI matrix or bump requires-python to >=3.9. |
| 7 | Add mypy/pyright to CI | DevEx | 2 | 3 | 2 | 2.5 | py.typed marker promises type safety — CI should enforce it. |
| 8 | Migrate test imports to `data_hygiene_auditor` | Tests | 1 | 3 | 2 | 2.0 | Tests should exercise the package, not the backward-compat shim. |
| 9 | Warn when fuzzy matching is skipped (>500 rows) | UX | 3 | 2 | 1 | 5.0 | User should know a detection pass was omitted on large sheets. |
| 10 | Scale fuzzy matching beyond 500 rows | Performance | 4 | 3 | 3 | 2.3 | Locality-sensitive hashing or blocking strategy to handle 10K+ rows. |
| 11 | Add CHANGELOG and release tagging | DevEx | 3 | 2 | 1 | 5.0 | Version tracking for users. Signal active maintenance. |
| 12 | PyPI publication | Reach | 5 | 1 | 2 | 3.0 | `pip install data-hygiene-auditor` from anywhere. Major discoverability boost. |
| 13 | "Data linter" positioning + README refresh | Reach | 4 | 1 | 2 | 2.5 | Adopt the "linter for data" framing that resonates with the developer audience. Keywords for discoverability. |
| 14 | File size guard / row limit warning | Security | 2 | 2 | 1 | 4.0 | Warn at 500K rows, refuse at 2M unless `--force`. Prevents OOM. |
| 15 | Remove `_load_sheets` from public `__all__` | Architecture | 1 | 2 | 1 | 3.0 | Private helper shouldn't be in the public API surface. |

### Recommended Sequence

**Sprint 5: Bug Fixes & Polish (half day)**
Moves #1-6, #9, #11, #14, #15. All effort-1 items. Brings the project to "no rough edges" state.
- Fix CLI counting bug
- Fix `_raw` type safety
- Extract issue-counting helper
- Document `--schema`/`--baseline` in README
- Add `--version` and `--quiet`
- Align Python version requirement
- Warn on skipped fuzzy matching
- Add CHANGELOG
- File size guard
- Remove `_load_sheets` from `__all__`

**Sprint 6: Engineering Rigor (1 day)**
Moves #7, #8. Type checking + test migration.
- Add mypy/pyright to CI
- Migrate test imports to package

**Sprint 7: Reach (1-2 days)**
Moves #12, #13. Get the tool in front of users.
- Publish to PyPI
- README refresh with "data linter" positioning

**Sprint 8: Scale (2-3 days)**
Move #10. Requires algorithmic work.
- Scale fuzzy matching with LSH or blocking

### What NOT to Do (2026 Update)

Previous "don't do" items that were done anyway and **worked out:**
- ~~Don't add schema validation~~ → Added (PR #9). Lightweight, optional, complements rather than competes with GX/pandera. **Correct call to add it.**

Updated guidance:
- **Don't add statistical profiling.** fg-data-profiling still owns this despite the rebrand. Your strength is consulting-specific findings.
- **Don't build a web app.** The interactive HTML file is self-contained, shareable, and zero-deployment. A server-side app is a different product for a different audience.
- **Don't chase pipeline integration.** The market moved further toward warehouse-native observability (DQOps, Soda, GX Cloud). That's their game. Yours is file-native audit reports.
- **Don't add LLM-powered features yet.** The deterministic fix suggestions already work well. LLM adds latency, API key requirements, and cost for marginal improvement. Revisit when local models are fast enough to run offline.
- **Don't over-engineer the fuzzy cap.** The 500-row Levenshtein cap is a reasonable default for spreadsheet-sized data. Add a warning, not a complex distributed algorithm. Only invest in scaling if real users hit the limit.
- **Don't compete on star count or downloads.** The niche is small but uncontested. One glowing testimonial from a consultant who used it on a real engagement is worth more than 1K GitHub stars from drive-by visitors.

### Strategic Summary

The project has successfully executed its transformation from "Claude Chat artifact" to "genuinely differentiated tool." The 2025 audit's thesis — that the detection was the moat but needed a stage — has been validated. The stage is now built. The next phase is about **credibility and reach**: fixing the remaining rough edges, publishing to PyPI, and positioning the tool where its target audience (consultants, analysts, data teams inheriting messy spreadsheets) can find it.

The competitive landscape has moved *away* from this project's niche (toward warehouse observability and data contracts), which is strategically favorable — it means less competition, not more. The window for "file-native, consultant-focused, severity-rated audit reports" remains wide open with no credible competitor in 2026.
